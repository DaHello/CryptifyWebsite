import { addLogById } from "../../actionsDB"; // for logs

// this button can be used in both file and text encryption
export const EncSubmit = () =>{
    return (
        <button type="submit" className="fButton" >Encrypt</button>
    );
}